# GPT

> GPT 是一个 decoder-only 的 Transformer，自回归语言模型，用来“生成下一个 token”。

## GPT 和 BERT 的根本区别

- **BERT**：encoder-only，双向，看全文，做理解
- **GPT**：decoder-only，单向，看左边，做生成

## 模型

![有点像decoder](https://cdn.jsdelivr.net/gh/salt235/tuchuang@main/img/QQ_1769496138099.png)

